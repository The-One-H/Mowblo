import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Jobs = () => {
  return (
    <SafeAreaView>
      <View>
        <Text>Jobs</Text>
      </View>
    </SafeAreaView>
  );
};
export default Jobs;
